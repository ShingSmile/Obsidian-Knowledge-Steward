
### 业务理解类

1. 为什么选择做"治理"而不是做一个更通用的知识问答 chatbot？
2. 你定义的"治理"具体包含哪些操作？边界在哪？
3. Obsidian 笔记有什么特殊性，跟普通文档管理有什么不同？
4. 为什么治理操作必须经过人工审批，不能自动执行？
5. 这个项目的目标用户是谁？个人用还是团队用？

### Agent 架构类

6. 你说选了 workflow-first 而不是 ReAct，具体区别是什么？在你的场景下 ReAct 会有什么问题？
7. 为什么自研 graph runner 而不是直接用 LangGraph？
8. 你的 LangGraph 兼容 shim 具体兼容到什么程度？条件分支支持吗？
9. checkpoint 存在 SQLite 里，如果要支持多实例部署怎么办？
10. 三条工作流（ask/ingest/digest）之间有没有共享状态？怎么隔离的？
11. 如果未来要加一条新的工作流，改动量有多大？

### Prompt / Workflow 类

12. 你的 prompt 模板是怎么管理的？有没有版本控制？
13. ask 链路里模型被调用了几次？每次的职责分别是什么？
14. 你怎么控制模型输出的格式？如果模型不按格式返回怎么办？
15. ingest 的治理 proposal 是模型生成的还是规则生成的？
16. digest 的复盘内容是怎么生成的？会不会出现重复总结？

### 检索与 RAG 类

17. RRF 的 k=60 是怎么确定的？有没有做过对比实验？
18. FTS5 对中文的支持怎么样？你用了什么分词方案？
19. 向量检索用的什么 embedding 模型？维度多少？
20. 检索结果最多返回几条？为什么是这个数？
21. context assembly 的 token 预算 2400 是怎么定的？
22. 如果笔记库规模扩大 10 倍，检索性能会怎样？

### Tool Calling 类

23. 你说有 5 层防御，如果模型绕过白名单强行输出一个不存在的工具名会怎样？
24. 为什么所有工具都是 read-only？如果治理场景需要写操作工具呢？
25. 工具的 JSON Schema 校验具体验什么？有没有遇到过模型输出不合法参数的情况？
26. 如果工具执行超时怎么处理？

### 稳定性与容错类

27. cloud provider 挂了切 local，两个 provider 的模型能力差异怎么处理？
28. citation alignment 校验具体检查什么？编号 [1] [2] 是怎么对齐的？
29. 你说 guardrail 会降级而不是报错，降级后用户体验会怎样？
30. trace hook 异常吞掉后，你怎么知道 trace 丢了数据？

### 效果评测类

31. 21 条 golden case 够吗？覆盖率你怎么衡量？
32. 你的评测只有确定性断言，没有语义级评测吗？怎么评估回答质量？
33. eval 有没有接 CI？每次提交都跑吗？
34. 如果要评估检索召回率，你会怎么做？

### 成本与性能类

35. 单次 ask 请求会调几次模型？token 消耗大概多少？
36. 你有没有做过延迟优化？瓶颈在哪？
37. 如果要降低模型调用成本，你会怎么做？
38. SQLite 单库承载所有数据（索引、checkpoint、trace、proposal），有没有瓶颈？

### 工程化与可维护性类

39. 你用的是原生 urllib 而不是 requests 或 httpx，为什么？
40. 为什么用 SQLite 而不是 PostgreSQL？
41. 后端 104 条测试的覆盖范围是什么？有没有集成测试？
42. 你的代码里有很多中文注释，这是有意为之的吗？

### 个人贡献真实性类

43. 这个项目是你一个人做的还是团队做的？
44. 项目有没有上线？用户量多少？
45. 你说有 49 个版本迭代，是在多长时间内完成的？
46. 这个项目的代码量大概多少？
47. 你有没有参考过其他开源项目？哪些部分是原创设计？
