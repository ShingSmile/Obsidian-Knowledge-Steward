# Task Planner

- [x] 运营端权限 ✅ 2023-12-12
- [x] 埋点规划 ✅ 2023-12-15

***
# Long Target

[保持习惯](保持习惯.md)

[待办清单](待办清单.md)

***
# Day Life

* 09:32 上班！
* 14:26 运营端加上了权限控制，然后大致把埋点的初步规划弄完了，提pr看看有没有问题，先看一下自营按批次相关的
```
SELECT t1.*, t3.* FROM `cxm_bd_role` t1
JOIN cxm_bd_role_permission t2
JOIN cxm_bd_permission t3
ON t1.id = t2.role_id AND t2.permission_id = t3.id
WHERE t1.is_admin = 3
ORDER BY t1.id;
```

```
SELECT csgr.stall_id FROM cxm_stall_group_relation csgr
JOIN cxm_bd_stalls cbs
ON csgr.stall_id = cbs.id
WHERE csgr.group_no = '528224838000214016' AND csgr.`enable` = 1 
AND cbs.market_id = 1 AND cbs.`name` LIKE '%菜%'
ORDER BY csgr.create_time DESC
```

* 17:06 看了三张表，两张表要略微调整一下，还剩下两张表没看，运营权限的sql过了，但是埋点还需要重新思考一下
* 21:42 代支代付原自营批次在入库的时候可以添加带有批次id的数据，所以不能直接batch_id>0，需要查批次表

***
# Summary

* 

## 每天进步一点点

* 



