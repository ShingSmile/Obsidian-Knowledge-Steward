# Task Planner

- [ ] 解决方案

***
# Long Target

[保持习惯](保持习惯.md)

[待办清单](待办清单.md)

***
# Day Life

* 14:19 上班！
* 14:19 解决方案基本定了，看一下有没有问题吧


***
# Summary

```
CREATE TABLE `cxm_batch_statistics` ( `id` int(11) NOT NULL AUTO_INCREMENT, `stall_id` int(11) NOT NULL DEFAULT '0' COMMENT '商行id', `category` varchar(32) NOT NULL DEFAULT '' COMMENT '', `code` varchar(32) NOT NULL DEFAULT '' COMMENT '', `enable` tinyint(1) DEFAULT '1' COMMENT '是否有效', `create_time` bigint(20) NOT NULL COMMENT '创建时间', `create_uid` int(11) NOT NULL COMMENT '创建人', PRIMARY KEY (`id`), ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='';
```


## 每天进步一点点

* 



