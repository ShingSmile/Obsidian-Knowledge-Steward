# Task Planner

- [x] 修改批次号检查 ✅ 2023-09-20

***
# Long Target

[保持习惯](保持习惯.md)

[待办清单](待办清单.md)

***
# Day Life

* 09:10 上班！
* 09:32 以前的搜索还有漏洞
	* com.cxm.service.order.BdSaleOrderBaseService#getOrderHeadWithItem检查方法
	* 把表的字段删除
* 11:22 目前准备先改一些，找找联系，再补漏洞
* 11:22 修改思路
	* 过滤批次号
	* 查询对应的车次号
	* 循环遍历填进去
```
List<Integer> batchIdList = trackList.stream().map(DkSaleOrderTransientTrack::getBatchId).filter(batchId -> batchId > CxmCommonConstant.CONSTANT_ZERO).collect(Collectors.toList());  
if(CollectionUtils.isNotEmpty(batchIdList)) {  
   Map<Integer, Integer> batchIdCarNoMap = cxmBatchBaseService.getIdCarNoMap(stallId, batchIdList);  
   for(DkSaleOrderTransientTrack track : trackList) {  
      track.setBatchCarNo(NumberUtils.validateInteger(batchIdCarNoMap.get(track.getBatchId())));  
   }  
}
```



***
# Summary

* 几种方式
	* DkSaleOrderVo或继承DkSaleOrderVo最终继承DkSaleOrderHead
* 性能优化
	* java层面
		* 循环依赖
		* 排序
		* 多重循环
		* 序列化
		* 循环内查询
	* 数据库、redis一定要处处注意
	* 将精力放在有更大影响的地方
* 首拼排序
	* 不要在排序中get首拼，可以先取出来再排序
* 今天批次号检查得差不多了，但是改的进程三分之一左右，还有地方要查缺补漏，明天希望可以顺利弄完，需要查询一下那几个get方法的调用，还有前端直接返回字段的地方

## 每天都有进步嘛

* 



