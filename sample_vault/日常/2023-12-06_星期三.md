# Task Planner

- [x] 增值服务订单列表 ✅ 2023-12-11

***
# Long Target

[保持习惯](保持习惯.md)

[待办清单](待办清单.md)

***
# Day Life

* 13:45 根据手机号查姓名有点麻烦
```
List<String> phoneList = new ArrayList<>();  
Map<Integer, List<String>> stallPhoneMap = new HashMap<>();  
scanpayRecordList.forEach( item -> {  
phoneList.add(item.getPhone());  
List<String> tempPhoneList = stallPhoneMap.getOrDefault(item.getStallId(), new ArrayList<>());  
tempPhoneList.add(item.getPhone());  
stallPhoneMap.put(item.getStallId(), tempPhoneList);  
});  
CxmBdUserExample example = new CxmBdUserExample();  
example.createCriteria().andAccountIn(phoneList).andTypeIn(Arrays.asList(UserTypeConstant.STALL_MANAGER, UserTypeConstant.STALL_EMPLOYER));  
List<CxmBdUser> userList = cxmBdUserService.selectByExample(example);  
List<Integer> managerList = new ArrayList<>();  
List<Integer> employList = new ArrayList<>();  
userList.forEach( user -> {  
if (Objects.equals(user.getType(), UserTypeConstant.STALL_MANAGER)) {  
managerList.add(user.getPersonId());  
} else {  
employList.add(user.getPersonId());  
}  
});  
List<CxmBdStallManager>
```
* 真傻了，createUid不就可以直接拿到付款人手机号和姓名，那张表存储的phone是干扰数据
* 18:46 订单列表还是需要修改

***
# Summary

* 四个问题
	* select不要用大写，不要用驼峰
	* 三个层级需要修改一下
		* 排序参数记得不要抽象
		* 逻辑=代码可读
	* businessType类型的描述需要修改一下
	* 查询的时间范围需要确认一下
		* 先了解需求，再综合其他影响沟通需求达成共识
	* person表直接查

## 每天进步一点点

* 



