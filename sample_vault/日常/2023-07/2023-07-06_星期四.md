# 一、工作任务

- [x] 1、代卖收益代码完善 ✅ 2023-07-06
- [ ] 2、检查批次状态
- [ ] 3、提测范围

# 二、学习目标

- [x] 1、掘金学习 ✅ 2023-07-06
- [ ] 2、力扣练习
- [ ] 3、 

***

# 三、今日进程

* 



***

# 四、今日总结

```
//获取结算费用  
    private List<CxmBatchFinance> getSettleBatchFinanceList(Integer stallId, Long startTime, Long endTime, List<String> dynamicFieldList, Integer supplierId){  
        List<Integer> allSettleBatchFinanceIdList = new ArrayList<>();  
  
//        QueryWrapper<CxmBatchFinance> queryWrapper = new QueryWrapper<>();  
//        queryWrapper.eq("stall_id", stallId).in("code", dynamicFieldList).eq(Objects.nonNull(supplierId) ,"supplier_id", supplierId)  
//                .between("create_time", startTime, endTime).eq("enable", true);  
//        List<CxmBatchFinance> allBatchFinanceList = batchFinanceBaseService.list(queryWrapper);  
//        if(CollectionUtils.isEmpty(allBatchFinanceList)){  
//            return new ArrayList<>();  
//        }  
//        List<Integer> batchIdList = allBatchFinanceList.stream().map(CxmBatchFinance::getBatchId).distinct().collect(Collectors.toList());  
//  
//        //查询出老数据的结算费用，全部结算  
//        LambdaQueryWrapper<CxmBatchSettlementBill> settleBillWrapper = new LambdaQueryWrapper<>();  
//        settleBillWrapper.eq(CxmBatchSettlementBill::getStallId, stallId)  
//                .eq(CxmBatchSettlementBill::getEnable, CxmCommonConstant.CONSTANT_TRUE)  
//                .in(CxmBatchSettlementBill::getBatchId, batchIdList);  
//        List<CxmBatchSettlementBill> settlementBillList = cxmBatchSettlementBillService.list(settleBillWrapper);  
//        List<Integer> oldSettleBatchIdList = settlementBillList.stream().map(CxmBatchSettlementBill::getBatchId).collect(Collectors.toList());  
//        List<Integer> oldSettleBatchFinanceIdList = allBatchFinanceList.stream().filter(item -> oldSettleBatchIdList.contains(item.getBatchId())).map(CxmBatchFinance::getId).collect(Collectors.toList());  
//        allSettleBatchFinanceIdList.addAll(oldSettleBatchFinanceIdList);  
        List<CxmBatchFinance> allBatchFinanceList = new ArrayList<>();  
        LambdaQueryWrapper<CxmBatchSettlementBill> settleBillWrapper = new LambdaQueryWrapper<>();  
        settleBillWrapper.eq(CxmBatchSettlementBill::getStallId, stallId)  
                .eq(CxmBatchSettlementBill::getEnable, CxmCommonConstant.CONSTANT_TRUE)  
                .ge(CxmBatchSettlementBill::getCreateTime, startTime)  
                .le(CxmBatchSettlementBill::getCreateTime, endTime);  
        List<CxmBatchSettlementBill> settlementBillList = cxmBatchSettlementBillService.list(settleBillWrapper);  
        List<Integer> oldSettleBatchIdList = settlementBillList.stream().map(CxmBatchSettlementBill::getBatchId).collect(Collectors.toList());  
        List<Integer> oldSettleBatchFinanceIdList = allBatchFinanceList.stream().filter(item -> oldSettleBatchIdList.contains(item.getBatchId())).map(CxmBatchFinance::getId).collect(Collectors.toList());  
        allSettleBatchFinanceIdList.addAll(oldSettleBatchFinanceIdList);  
  
        //新数据费用部分结算  
        List<CxmBatchFinance> settleBatchFinanceList = cxmBatchFinanceBaseService.getSettleBatchFianceList(stallId, dynamicFieldList, supplierId, startTime, endTime);  
        List<Integer> settleBatchFinanceIdList = settleBatchFinanceList.stream().map(CxmBatchFinance::getId).collect(Collectors.toList());  
        allSettleBatchFinanceIdList.addAll(settleBatchFinanceIdList);  
        allBatchFinanceList = allBatchFinanceList.stream().filter( item -> allSettleBatchFinanceIdList.contains(item.getId())).collect(Collectors.toList());  
        return allBatchFinanceList;  
    }
```

```
<select id="getSettleBatchFianceList"  resultType="com.cxm.db.model.batch.CxmBatchFinance">  
    SELECT bf.id, bf.supplier_id, bf.batch_id, bf.code, bf.amount, bf.type  
    FROM    cxm_batch_finance bf    left join cxm_settlement_finance_relation sfr    on bf.id = sfr.finance_id and sfr.stall_id = #{stallId,jdbcType=INTEGER} and sfr.enable = 1 and sfr.finance_type = 1    WHERE    bf.stall_id = #{stallId,jdbcType=INTEGER}    <if test="supplierId != null">  
        and bf.supplier_id = #{supplierId,jdbcType=INTEGER}  
    </if>  
    and bf.code in <foreach collection="codeList" item="code" open="(" separator="," close=")">#{code}</foreach>  
    and bf.enable = 1  
    <![CDATA[ and bf.create_time >= #{startTime,jdbcType=NUMERIC} ]]>    <![CDATA[ and bf.create_time <= #{endTime,jdbcType=NUMERIC} ]]>    and sfr.id is not null</select>
```