# 一、工作任务

- [x] 1、代卖收益部分 ✅ 2023-07-05
- [x] 2、月会 ✅ 2023-07-05
- [ ] 3、联调会

# 二、学习目标

- [ ] 1、掘金学习
- [ ] 2、力扣练习
- [ ] 3、 

***

# 三、今日进程

* 把昨天的货主管理pr提上去了 截至9：45
* 



***

# 四、今日总结

* 级联更新、外键、触发器等一些理论很少在实际情况中发挥作用（消耗性能）
* 线上难免有bug产生一些bug数据
	* 处理方式1：如果数据库严格设计没有冗余字段，可以通过关联表的方式查询出正确数据
	* 处理方式2：有冗余字段，且bug数据量不太大的情况下，可以刷数据
* 空格展示代码思路逻辑
* 几个联表查询的放到结算service里面去
* 代卖收益
	* 获取货品附加费（不变）
	* 获取结算费用
		* 原直接查询结算费用表，现老数据批次（结算单表中有结算数据的）直接查询结算费用表，新数据批次要联表查询已经结算的结算费用
	* 获取结算单的summarydata中附加费（代卖费）
		* 从新结算单头部表的financeinfo数据
* service层的校验如果是是否执行的，应该在外层manager判断，只有是不同分支的才放到service内部

```
//        List<Integer> filterBatchFinanceIdList = allBatchFinanceList.stream()  
//                .filter(item -> !oldBatchIdList.contains(item.getBatchId()))  
//                .map(CxmBatchFinance::getId).collect(Collectors.toList());  
//        LambdaQueryWrapper<CxmSettlementFinanceRelation> settleFinanceRelationWrapper = new LambdaQueryWrapper<>();  
//        settleFinanceRelationWrapper.eq(CxmSettlementFinanceRelation::getStallId,stallId)  
//                .eq(CxmSettlementFinanceRelation::getEnable, CxmCommonConstant.CONSTANT_TRUE)  
//                .eq(CxmSettlementFinanceRelation::getFinanceType, FinanceTypeConstant.BATCH_FINANCE)  
//                .in(CxmSettlementFinanceRelation::getFinanceId, filterBatchFinanceIdList);  
//        List<CxmSettlementFinanceRelation> settleFinanceRelationList = cxmSettlementFinanceRelationService.list(settleFinanceRelationWrapper);  
//        List<Integer> settleBatchFinanceIdList = settleFinanceRelationList.stream().map(CxmSettlementFinanceRelation::getFinanceId).collect(Collectors.toList());
```