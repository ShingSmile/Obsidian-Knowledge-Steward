# 一、工作任务

- [x] 1、批次结算、货主管理列表接口提供 ✅ 2023-06-20
- [ ] 2、批次结算列表开发
- [ ] 3、

# 二、学习目标

- [ ] 1、掘金学习
- [ ] 2、力扣练习
- [ ] 3、 

***

# 三、今日进程

* 上午在修改接口
* 下午基本完成了app、pos端批次结算列表
	出现问题：@Autowired CxmBatchBaseService一直为null，
	原因：外层manager没有@Autowired，浪费时间！

if(query.getSaleStatus() != null ){  
    if(query.getSaleStatus() == 0){  
        // 售罄按售罄时间降序  
        batchStatus.add(BatchStatusEnum.SELL_OUT_OPEN.getStatus());  
        queryWrapper.orderByDesc("end_time");  
    } else{  
        // 在售按创建时间降序  
        batchStatus.add(BatchStatusEnum.ON_SALE_OPEN.getStatus());  
        queryWrapper.orderByDesc("create_time");  
    }  
}  
if(query.getSettlementStatus() != null){  
    if(query.getSettlementStatus() == 1){  
        batchStatus.add(BatchStatusEnum.SELL_OUT_CLOSE.getStatus());  
        batchStatus.add(BatchStatusEnum.ON_SALE_CLOSE.getStatus());  
        // 结完按结完时间降序  
        queryWrapper.orderByDesc("settle_time");  
    }  
}


***

# 四、今日总结

* 